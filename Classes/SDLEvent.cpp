#include "SDLImp.h"

/*
#if __cplusplus
extern "C" {
#endif
#include <SDL.h>
#if __cplusplus
}
#endif

namespace ff{
	int PushEvent(Event *event)
	{
		return SDL_PushEvent((SDL_Event*)event);
	}

	int PeepEvents(Event *events, int numevents,
		eventaction action, Uint32 mask)
	{
		return SDL_PeepEvents((SDL_Event *)events, numevents, (SDL_eventaction)action, mask);
	}

	void PumpEvents(void)
	{
		SDL_PumpEvents();
	}
}
*/